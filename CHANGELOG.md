# Changelog

本文件记录本项目的所有重要变更。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

> 说明：`0.0.0` 及其之前的历史以「初始开发」集中记录，未按版本细分。

## [Unreleased]

### 新增

- **Markdown 感知粘贴**：粘贴模式新增 `'markdown'`，把剪贴板中的 Markdown 源码解析为富文本（标题、列表、表格、行内标记等），而不是作为字面文本插入。工具栏按钮改为在 `plain` → `structured` → `markdown` 之间循环切换。
- **`insertMarkdown(markdown)` 命令**：把 Markdown 解析后插入到当前选区。
- **纯文本导出 API**：新增 `session.getText()`，返回块级以换行分隔的纯文本，图片取 `alt` 文本。适合字数统计、搜索摘要、复制为纯文本等场景。
- **文本对齐**：新增 `setTextAlign(alignment?)` 命令与工具栏分组，支持左/中/右/两端对齐，不传参时清除对齐。Markdown 本身无对齐语法，因此以带内联样式的 HTML 块持久化，可在 `getMarkdown()` → `setMarkdown()` 往返中保留。
- **缩进**：新增 `indent()` / `outdent()` 命令与工具栏分组。列表内下沉/提升列表项，列表外调整段落与标题的缩进层级（上限 8 级），同样支持 Markdown 往返。
- 新增导出类型 `TextAlignment`。

### 变更

- `PasteMode` 类型从 `'plain' | 'structured'` 扩展为 `'plain' | 'structured' | 'markdown'`。
- 段落与标题节点新增 `indent` 属性。
- 工具栏新增 `align` 与 `indent` 分组，以及 `align-left`、`align-center`、`align-right`、`align-justify`、`indent`、`outdent` 六个默认条目。
- 工具栏按钮新增 `data-nexusdown-command` 属性，便于测试与自动化选择。

### 修复

- **打包缺陷**：`nexusdown/vue` 之前指向裸 `.vue` 文件，任何真实安装都无法解析（`ERR_PACKAGE_PATH_NOT_EXPORTED` / `ERR_UNKNOWN_FILE_EXTENSION`）。现在指向 `src/vue/entry.js`（由 `entry.ts` 生成），并提供 `types` 与 `default` 条件。
- **样式分叉**：`public/style.css` 与 `src/style.css` 曾各自演进并丢失 38 条规则；现在 `src/style.css` 为唯一真源，`npm run build` 自动同步，并有测试逐字节锁定。
- **查找替换索引残留**：删除全部匹配后 `currentIndex` 保留陈旧值（如 `matches: 0, currentIndex: 2`），现无条件钳制。
- **标题菜单无法关闭**：`HeadingPicker` 缺少外部点击与 `Escape` 处理，弹出后只能再次点击触发器关闭；同时修复关闭后未解绑的全局监听器。
- **移动端适配**：输入框提升到 16px（避免 iOS 聚焦缩放）、触控目标 ≥44px、小屏高度自适应、`env(safe-area-inset-bottom)` 安全区、禁用触屏粘滞 `:hover`。
- **菜单定位**：链接/图片/标题菜单与查找面板改用 `visualViewport`，避免软键盘弹出后与触发器脱节或被遮挡。
- **切换布局后富文本区空白**：切换 `layout` 会经过不同的 `v-if` 分支，Vue 丢弃了 Tiptap 挂载的 DOM 节点，导致富文本区变空且无法编辑。现在在布局变化后重新挂载到当前节点，文档内容保持不变。（issue #1）
- **编辑已有链接时文字被拆分**：光标位于链接内部时设置链接会直接插入新文字，把原链接切成 `[d]新文字[ocs]`。现在会扩展到整段链接后整体替换，选中部分链接时同样覆盖完整链接。（issue #1）
- **无效初始 JSON 导致组件无法挂载**：`contentType="json"` 且值非法时，`JSON.parse` 在 setup 阶段抛出，组件不会渲染，声明的 `parse-error` 事件也永远不会触发。现在回退为空文档并正常抛出 `parse-error`。（issue #1）
- **Markdown 侧选区未同步给工具栏**：在 Markdown 面板选中文字后，共享工具栏仍读取富文本侧选区，格式命令会作用到错误的一侧。现在 `MarkdownEditor` 会上报选区，工具栏优先采用它，选区折叠时回退到富文本选区。（issue #1）
- **重复挂载导致 EditorView 泄漏**：会话在构造时挂载了一个临时节点，组件初始化又直接 `mount()` 而未先卸载，旧 `EditorView` 及其插件不会被销毁，`create` 初始化回调也会重复执行；默认首次加载即可触发。新增 `session.mountEditor(element)`，切换挂载目标前先卸载，对同一目标重复调用为幂等操作。（issue #1 评论）

### 修复 — Markdown 转义（数据保真）

这一组问题的共同后果是：**保存后再载入，内容含义被改变**。已逐条以「HTML → Markdown → 重新解析 → HTML」往返复现并验证修复。

- **行首块级标记未转义**：普通段落以字面量 `# `、`- `、`1. `、`>` 开头时，导出的 Markdown 会被重新解析成标题／列表／引用。现在仅对行首做转义，行中出现同样字符不受影响。其中有序列表必须转义**分隔符**（`1\. `）而不是数字（`\1. `）：反斜杠只能转义 ASCII 标点，`\1` 不是有效转义，会原样留下一个反斜杠。
- **表格单元格中的 `|` 未转义**：单元格内容里的竖线会被当作列分隔符，导致后续列整体错位、超出表头列数的内容被静默丢弃。上游 `@tiptap/extension-table` 只在解析时转义（`preprocessTablePipes`）、渲染时不转义，现于渲染阶段补齐。
- **代码块围栏长度固定**：代码正文含独占一行的三个反引号时会提前闭合代码块，后续内容被当作正文。现在围栏按内容中最长的反引号串自动加长。
- **图片 `alt` 中的 `]` 未转义**：未配对的 `]` 会提前结束标签，整张图片退化成普通文本加一个链接。
- **链接目标中的括号未转义**：地址含未配对括号时会截断目标地址；现已按 CommonMark 以尖括号包裹，普通 URL 保持原样。
- **`==` / `++` 未转义**：普通文字中的字面量 `==文本==`、`++文本++` 会被重新解析成高亮／下划线。仅转义未带对应标记的文本，真实标记不受影响。
- **高亮内容含单个 `=` 时无法解析**：上游高亮分词器使用 `[^=]+`，结构上无法匹配 `==a=b==`，导致高亮丢失并留下原始标记。已放宽分词规则使其可被识别。

**已知限制**：行内代码包含反引号时仍固定使用单反引号包裹。`@tiptap/markdown` 渲染 *mark* 时传入的是仅含固定占位符的合成节点，因此 `code` 的 stock 渲染器无法依据内容决定围栏长度——这一点已核对库源码确认。但需要说明的是，「无法通过公开 API 修复」的说法并不准确：库提供了公开的 `markdownOptions.htmlReopen`（见 `@tiptap/core` 类型声明）可以让 mark 改用 HTML 标签输出，从而绕开占位符路径；本项目选择不使用它，属于**范围决策**而非 API 限制。该限制已由测试显式记录。
- **每次按键的冗余序列化**：编辑器 `update` 不再无条件执行 Markdown + HTML + JSON 三次全量序列化。
- **能力探测副作用**：移除 `can()` 内部无意义的 `.focus()` 调用。
- **死代码**：清理 `MarkdownEditor.vue` 中恒真的注册守卫与 `useNexusdownTheme.ts` 未使用的导入。

### 修复 — 会话状态一致性

- **订阅者异常会中断后续通知**：通知订阅者时使用的是普通循环，某个回调抛错会直接终止整个循环，排在后面的订阅者收不到更新、继续持有旧文档。现在逐个隔离，异常改由错误通道上报，且快照先提交再通知，避免回调抛错导致会话状态不一致。
- **订阅回调重入会送出已过期的快照**：订阅者可以在自己的回调里修改内容，该修改会在当前这一轮通知尚未结束时重入通知逻辑。此前外层循环会继续把**旧**快照发给排在后面的订阅者，按最后一次通知保存的宿主可能用旧内容覆盖新内容。现在重入时记录新快照并中止当前这一轮，改为从头部重新广播，保证只有最新快照会送达（已订阅过旧快照的也会被刷新）。
- **未知节点类型的 JSON 被当作正文显示**：ProseMirror 不会拒绝无法使用的文档，而是把它降级成一个装着原始 JSON 的文本节点，用户会看到一段 JSON 源码，且不报任何错误。现在构造和 `setContent` 都会先校验（未知节点类型、缺少 `doc` 根、违反结构约束），拒绝时回退为空文档并通过错误通道上报。同时，JSON 字符串会被解析成文档结构，而不是当作字面文本。
- **解析失败回滚会清空重做栈**：回滚使用普通 `setContent`，其事务会进入历史记录，导致重做记录被清空、撤销栈里多出一条无意义记录。现在以 `addToHistory: false` 的事务恢复内容，文档回到原状且历史栈不受影响。
- **快捷键撤销未标记为历史来源**：只有会话自身的 `undo()`／`redo()` 会设置来源，用 Ctrl/Cmd+Z 撤销时来源仍是 `rich-text`，依赖来源判断的保存逻辑会把历史操作误判为普通编辑。现在改为识别 ProseMirror 历史插件事务上的 `history$` 标记。

### 修复 — 链接标题丢失与命令健壮性

- **链接的 `title` 未写入 Markdown**：`Link` 标记本身带 `title` 属性（悬停提示），但渲染时只输出 `[文本](地址)`，标题在每次导出时被静默丢弃（图片分支一直有处理，链接分支遗漏）。现在按与图片相同的规则输出并转义引号与反斜杠。
- **未配置参数的图片按钮抛错**：`insertImage` 直接调用 `src.trim()`，当宿主渲染图片按钮却未提供 `imageSrc` 时抛出 `TypeError: Cannot read properties of undefined (reading 'trim')`，并从工具栏点击处理函数中逃逸。现在 `src` / `alt` / `title` 均做类型校验，缺参数时按无操作返回 `false`。

### 修复 — 被拒绝的草稿残留

- **解析失败后 Markdown 输入框仍保留被拒草稿**：会话拒绝无法解析的输入（例如含 NUL 字符）并回滚文档，`v-model` 也随之回到上一次有效值，但输入框里仍是那段被拒绝的草稿——面板与富文本、绑定值三方不一致，用户看不出自己的输入已被拒绝。原因还有一层：`modelValue` 回到原本就相同的值时 Vue 认为「没有变化」，根本不会去更新 DOM。现在面板提供 `syncValueFromProp()`，父组件在拒绝后显式要求同步。

### 修复 — 工具栏图标状态不刷新

- **光标处用快捷键切换格式时工具栏不更新**：光标未选中任何文字时按下 `Ctrl/Cmd+B` 等快捷键只设置 ProseMirror 的 `storedMarks`——文档与选区都没变，因此既没有 `update` 也没有 `selectionUpdate`，而工具栏由会话的选区通知驱动，于是按钮仍显示未激活，但下一个输入的字符其实已经是粗体。现在这类事务会显式通知选区订阅者，且单个订阅者抛错不会影响其余订阅者。

### 修复 — 中文输入法下的回车误提交

- **图片弹窗在输入法组词时按下回车会立即提交**：图片地址与描述的输入框绑定 `@keydown.enter="apply"`，而中文／日文输入法中用于**确认候选词**的回车同样是一次 keydown，会直接应用并关闭弹窗，用户还没打完字就提交了。现在两个输入框都先检查 `isComposing`（并兼容旧浏览器的 `keyCode === 229`），组词期间的回车不再触发提交。链接弹窗的地址输入框存在同样问题，已按相同方式修复。

### 修复 — 表格列丢失不再静默

- **表格行多出的单元格被静默丢弃**：上游表格分词器按表头列数构建每一行，因此两列表头下写入 `| 1 | 2 | 3 |` 会变成 `| 1 | 2 |`——解析「成功」，没有任何提示，用户的内容就这么没了。现在会话会检测表头与各行的列数差异，并通过与其它解析错误相同的通道上报（说明行号与列数）。行数**少于**表头的情况不报警：解析器会补空格子，不丢内容。转义的 `\|` 不会被当作分列符，非表格的含 `|` 文本也不会误报。

### 修复 — 会话状态一致性（续）

- **`can()` 对未知命令返回 `undefined`**：`canInternal` 的 `switch` 没有 `default` 分支，未识别的命令名（例如从无类型 JavaScript 或模板传入 `'heading1'`）会穿透整个分支直接返回 `undefined`，与声明的 `boolean` 返回类型不符，也让错误静默通过。现在统一返回 `false`。

### 修复 — Markdown 面板输入被打断

- **输入的半成品标记被自动改写**：Markdown 面板每次会话快照都会把规范化后的文本写回输入框，因此正在输入的内容会被就地改写——输入 `see [link` 会立刻变成 `see \[link`，`*`、`` ` ``、`**` 等同理，导致这些字符几乎无法正常输入。现在来自 Markdown 面板自身的快照不再回写输入框，富文本侧驱动时行为不变。

### 修复 — 链接编辑与高亮颜色

- **链接文本含局部粗体时只显示片段**：链接标签混排样式时会被拆成多个文本节点（`ab` + 粗体 `cd` + `ef`），此前只读取光标所在的那一个节点，弹窗把 `ef` 当作完整链接文本，看起来像文字丢失。现在会合并整段连续的链接节点。
- **对选中文字添加链接会丢失原有样式**：替换选区时只写入链接标记，粗体／斜体／颜色等标记被静默丢弃。现在保留选区已有的全部标记，再把链接叠加其上。
- **自定义高亮颜色未写入 Markdown**：渲染时只输出 `==…==`，丢弃了 `color` 属性，自定义背景色在保存重载后退回默认值。现在按 `=={颜色}文本==` 输出并解析；未设置颜色时仍输出普通的 `==文本==`，历史文档不受影响。

### 已核实但不适用（非缺陷）

- **「`nexusdown/vue` 入口缺少具名导出」**：`exports["./vue"]` 指向生成文件 `src/vue/entry.ts`，其中已具名导出 `NexusdownEditor`、`EditorToolbar` 及各组件；`exports["./vue/*"]` 另有按路径引入 SFC 的通道。SFC 有意不预编译（否则会在消费方重复打包一份 Vue），该文件由 `npm run gen:vue-entry` 生成并保持同步，导出缺失一说不成立。
- **「SSR / 无 DOM 环境下初始化报错」**：实测创建会话与序列化均正常，未抛异常。
- **「解析失败仍触发自动保存」**：实测解析失败时不会发出订阅快照，输入框回退到上一次正常内容。
- **「多个编辑器共用全局 Marked 实例导致状态串扰」**：实测两个编辑器分别解析 `==hi==` 与 `**bold**` 互不影响。
- **「自定义节点／HTML 回退丢失内容」**：非 schema 内的标签按预期降级为段落文本，图片 `alt` 中的中文与特殊字符往返一致。
- **「表格控件缺少 ResizeObserver」**：项目并未使用 `ResizeObserver`，`TableControls` 通过 `resize` 与**捕获阶段**的 `scroll` 监听，加上选区／文档更新订阅来重新定位，`onBeforeUnmount` 中逐个对称移除（已加测试锁定：挂载时注册的每个监听器都会在卸载时移除，含捕获标志）。定位全部基于 `getBoundingClientRect()` 的相对差值并夹取到容器尺寸。
- **「气泡定位受 border-box 影响而偏移」**：所有浮层定位一律使用 `getBoundingClientRect()`，与 `box-sizing` 无关；代码中不存在 `offsetTop` / `offsetLeft` 定位，该条不成立。
- **「双栏滚动同步不可用」**：同步按两栏滚动范围的**比例**映射，带重入守卫与亚像素阈值以避免回弹；`syncScroll` 可关闭。实测滚动不抛异常，jsdom 零高度下也不会写入 `NaN`。
- **「标题菜单不响应外部点击或 Escape」**：`HeadingPicker` 在 `open` 变化时注册／移除 `pointerdown` 与 `keydown` 监听，外部按下或 Escape 均会关闭，落在菜单或触发器内部的按下不会误关。已加测试锁定六种情形。
- **「解析失败后 Markdown 输入框保留被拒草稿」**：确认为真实问题——输入含 NUL 等无法解析的内容时，`v-model` 已回到上一次有效值，但输入框里仍是被拒绝的草稿，两边不一致。见下方「修复 — 被拒绝的草稿残留」。
- **「编辑器首次初始化重复挂载」**：`mountEditor` 幂等——对同一元素重复调用只会保留一个 `EditorView`，宿主容器子节点数量为 1，旧视图不会泄漏。
- **「代码块正文含独占一行的三个反引号」**：围栏会自动升格为四个反引号（`` ```` ``），往返解析完全一致。
- **「多个会话共用全局 Marked 扩展导致互相影响」**：实测两个会话对同一段 `!!文本!!` 的解析结果一致，且在会话创建顺序改变后保持不变，未观察到全局状态串扰。
- **「颜色使用 `var(...)` 回退时内部文字多出反斜杠」**：实测回退文本与 `[color]` 分支的文字均无多余转义；RGB 等可表达颜色下的嵌套斜体可正常往返。仅在「不可表达颜色 + 内部嵌套格式」这一组合下存在已知限制，见下方说明。
- **「FontFamily 未写入 Markdown」**：本项目未注册 `FontFamily` 扩展，工具栏也没有字体选择项，schema 中不存在 `fontFamily` 属性；该条描述的功能并不存在，因此无从丢失。
- **「嵌套任务列表 schema 冲突」**：实测嵌套任务列表可以正常解析与回写（`- [ ] parent` / `  - [ ] child` 往返一致）。
- **「表格序列化压缩行内代码空白」**：Markdown／JSON 路径均完整保留 `` `a  b` ``；仅在手工传入 HTML 且由 ProseMirror 的 HTML 解析器处理时才会归一化空白，与本项目的表格序列化无关。
- **「`var(...)` 颜色产生多余反斜杠」**：Markdown 没有颜色语法，非常规颜色值本就按设计回退为内联 HTML，输出 `<span style="color: var(--x)">` 是预期行为，未产生多余转义。

### 已知限制（非缺陷）

- **Markdown 无法保留表格合并单元格**：`rowspan` / `colspan` 在导出时被展开为空格子，再从 Markdown 侧继续编辑会丢失合并关系。Markdown 语法本身没有合并单元格的表达方式，因此无法通过转义或序列化修复；需要保留合并信息时应以 HTML／JSON 作为存储格式。
- **无法用 Markdown 表达的颜色，内部嵌套格式会退化为字面标记**：颜色值不是十六进制／`rgb()` 等可表达形式时（例如 `var(--brand)`），会按设计回退为内联 `<span style="color: …">`；而 CommonMark **不会**处理行内 HTML 标签内的 Markdown，因此其中的 `**粗体**` 会原样显示为星号、`` `代码` `` 会跑到 `</span>` 之外。这一后果确实源于 CommonMark 的规则，无法靠转义解决。需要澄清两点：其一，同一原因也让**对齐／缩进块**里的所有行内标记退化为字面量（`**b**`、`` `c` ``、`==hi==`），这是同一处设计的连带影响，已单独补测记录；其二，库的 `markdownOptions.htmlReopen` 是**公开**字段（见 `@tiptap/core` 类型声明），因此不采用它属于**范围决策**，而非「依赖私有 API」的被迫取舍。颜色可由 `[color]` 短代码表达时不存在该问题，嵌套标记正常保留。

### 修复 — 链接标题丢失与命令健壮性
- **行内代码包含反引号时固定使用单反引号包裹**（详见上文「修复 — Markdown 转义」中的说明）。

### 修复 — 界面细节

- **占位提示不显示**：`Placeholder` 扩展已配置文字，但样式表缺少对应规则，空白富文本区看不到任何提示。现补上 `is-empty` / `is-editor-empty` 的 `::before` 规则（默认接入即生效，无需宿主额外补样式）。
- **原生模态对话框内菜单不可用**：链接／图片／标题菜单固定挂到 `body`，编辑器放在 `dialog.showModal()` 中时，菜单落在对话框 top layer 之外而被浏览器置为非交互，表现为被遮挡、输入框无法聚焦、按钮点击无效。现在检测到模态对话框祖先时改为挂载到该对话框内，普通页面与非模态对话框行为不变。
- **弹窗脱离主题作用域**：`--nexus-*` 主题变量只声明在 `.nexusdown-editor[data-nexusdown-theme]` 上，而菜单被传送到 `body`，因此暗色编辑器会弹出亮色菜单，宿主在实例上覆盖的 `--nexus-*` 也完全不生效。菜单不能改为挂载到编辑器内部（编辑器设了 `overflow: hidden`，`position: fixed` 的菜单会被裁掉），因此改为在定位时把解析后的主题变量同步到菜单元素上。
- **Markdown 高亮层与输入框换行不一致**：两层共用 `white-space: pre-wrap`，但高亮层缺少长词折行规则，连续无空格文本／长 URL 在输入框内换行、在高亮层却横向溢出，导致文字截断、光标与可见文字错位。现为两层统一补充 `overflow-wrap` / `word-break`。
- **对齐／缩进块中的 `==`／`++` 泄露反斜杠**：这类块没有 Markdown 语法，会回退为内联 HTML。转义哨兵在普通路径上会被换回 `\`（库的转义器随后消费掉），但 HTML 路径上没有任何转义器运行，于是哨兵直接变成可见的反斜杠——`<p style="text-align: center">==x==</p>` 往返后正文变成字面量 `\=\=x\=\=`。HTML 内文本本就是字面量、不需要转义，现改为**移除**哨兵而不是转换。

### 修复 — 工具栏与扩展兼容

- **自定义按钮无法通过类型检查**：`ToolbarItem.id` 与 `group` 是内置名称的封闭联合类型，为自定义扩展（如 `badge`）添加按钮会触发 TS2322，尽管运行时完全支持。现放开为「内置名称 + 任意字符串」，内置名称仍保留自动补全。
- **仅设置字体时文字颜色按钮误判为激活**：`textStyle` 标记被 `fontFamily` 等属性共用，`isActive('textStyle')` 对没有颜色的文字同样返回 true，导致颜色按钮显示为已激活、点击后反复执行清除分支，无法应用颜色。新增 `hasTextColor()` 专门判断颜色属性。
- **自定义标题级别时整个标题菜单被禁用**：`can('heading')` 固定探测 H2，当使用方配置的级别不含 H2（例如 `[1, 3]`）时该探测失败，整个标题菜单被禁用，尽管 H1／H3 均可正常使用。现在按实际配置的级别逐一探测。
- **标题项移出 `block` 分组后控件消失**：标题控件是下拉选择器而非普通按钮，由模板中的独立分支渲染，而该分支要求分组必须是 `block`；同时分组列表是写死的白名单，未登记的分组会被**静默丢弃**。于是把标题项放进别的分组（尤其是自定义分组）后按钮直接消失。现在标题选择器跟随标题项所在的分组渲染，分组顺序按内置顺序优先、自定义分组按声明顺序追加，不再丢项。
- **移除可选扩展后工具栏渲染报错**：`can()` 与各命令会调用可选扩展提供的链式方法，移除表格／任务列表／图片／颜色／高亮扩展后这些方法不存在，探测或点击对应按钮会抛出 `TypeError: chain.toggleTaskList is not a function`，导致工具栏渲染失败。现在命令不可用时返回 `false`（按钮置灰），不再抛错。`can()` 的整个分发、`toggleUnderline`／`toggleSuperscript`／`toggleSubscript`、`setLink` 的 `unsetLink` 分支，以及 `indent`／`outdent` 内部对 `listItem`／`taskItem` 的探测（`can().sinkListItem()` 会即时解析节点类型）都补齐了同样的保护。
- **高亮／下划线内容含 `=`、`+` 时标记丢失**：转义原先写在 mark 自己的渲染器里，但该渲染器只会拿到一个仅含占位符的合成节点，永远看不到真实内容，因此转义实际是**空操作**——`<mark>a==b</mark>` 导出为 `==a==b==`，重新解析成 `<mark>a</mark>b==`（标记被截断、多余定界符泄露进正文）。转义改到能看到真实文本的文本节点上；同时下划线此前完全没有自己的分词器，`\+` 会原样显示为 `\+`，现补齐并让其按单元消费转义；此外被转义过的内容不再二次分词，避免 `x==y==z` 解析成嵌套高亮。
- **空撤销／重做污染下一次编辑的来源**：`undo()`／`redo()` 在执行前就标记来源为 `'history'`，当没有可撤销的步骤时命令返回 `false` 且不会有 `update` 来清除标记，于是**下一次无关的普通编辑**被标成 `'history'`，按来源判断的保存逻辑会误判。现在只在命令真正成功后才标记。
- **颜色／高亮的清除绕过自定义回调**：颜色选择器的清除分支直接调用内置命令，跳过了 `ToolbarItem.execute`，使用方在回调中的校验、记录逻辑不执行，即使回调本应拒绝操作，已有格式仍会被清除。现在清除同样经过 `execute`。

### 修复 — 会话状态一致性（已核实不成立）

以下条目经复现**不成立**，未做改动，仅补充回归测试锁定当前行为：

- 传出「Markdown 相同时跳过快照更新」：Markdown 未变化但 HTML 变化时（例如只加颜色）仍会发出通知，快照同步更新。
- 传出「同一次编辑重复发送 `update:modelValue`」：实测一次编辑只发出一次。
- 传出「解析失败后 Markdown 输入框仍保留被拒绝的草稿」：实测输入框回退到上一次有效内容，且不发出 `update:modelValue`。

### 工程

- 新增 GitHub Actions CI：多 Node 版本（20/22/24）执行 typecheck、lint、测试、构建，并校验构建产物已提交。
- 新增消费者冒烟测试（`npm run test:consumer`）：真实 `npm pack` → 隔离安装 → 校验全部子路径可解析 → 用 Vite 构建真实应用（16 项检查）。这是唯一能捕获打包类回归的检查。
- 接入 ESLint（flat config，含 `typescript-eslint` 与 `eslint-plugin-vue`，0 error / 0 warning）与 commitlint（Conventional Commits，允许中文 subject，当前为提示性检查）。
- 新增 `CONTRIBUTING.md`，记录架构约束、开发须知、发布流程与易踩的坑。
- 新增 `.gitattributes`：统一按 LF 检出。此前在 `core.autocrlf=true` 的 Windows 上会检出为 CRLF，而构建脚本始终写 LF，会让 CI 的「构建产物已提交」检查在部分机器上误报。
- 移除 `.gitignore` 中的 `package-lock.json` 规则：该文件其实一直被 git 跟踪，而 CI 的 `npm ci` 依赖它，保留规则会造成「已忽略但仍被跟踪」的困惑状态。
- 新增 `scripts/generate-vue-entry.mjs`，由 TypeScript 源生成可被 Node 解析的入口 shim，并有测试锁定一致性。

## [0.0.0] - 初始开发

### 新增

- 基于 Tiptap 3 的双视图编辑器：核心会话保持纯 TypeScript，Vue 3 作为独立适配层。
- 格式能力：标题、引用、有序/无序/任务列表、代码块（Lowlight 高亮 + 语言选择）、表格（增删行列、拖拽列宽、合并/拆分单元格）、图片（URL / 本地上传 / base64 回退）、链接、颜色、高亮、下划线、上下标、Typography。
- 查找替换面板（`Ctrl/Cmd+F`）、粘贴模式切换、字符与行数状态栏。
- 主题支持：`light` / `dark` / `system`，通过 CSS 变量换肤。
- 可配置布局：`layout`（`rich-left` / `markdown-left`）、`width` / `height`、`syncScroll`。
- 自定义 Markdown 语法扩展点：`markdownTokenName` / `markdownTokenizer` / `parseMarkdown` / `renderMarkdown`。

[Unreleased]: https://github.com/NexusMC-CN/nexusdown/commits/main
[0.0.0]: https://github.com/NexusMC-CN/nexusdown/releases
